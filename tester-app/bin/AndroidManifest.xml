<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.apktester.test"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="16" />

    <instrumentation
        android:name="android.test.InstrumentationTestRunner"
        android:targetPackage="my.example.HelloWorld" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <uses-library android:name="android.test.runner" />
        
        <service android:enabled="true" android:exported="true" android:name=".DelayService" android:process=":delayprc"> 
         	<intent-filter>
            	<action android:name="my.apktester.test.START_SERVICE" />
        	</intent-filter>
          </service>
          k
    </application>

   
    
</manifest>